from setuptools import setup

setup(name='svar',
      version='1.0',
      description='SVAR is a simple standard for formatting data.',
      license='GNU',
      packages=['svar'],
      zip_safe=False)
